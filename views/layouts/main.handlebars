<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Echo Chamber Media</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Welcome to the Echo Chamber!</h1>
  </header>
  {{#if loggedIn}}
  <div class="logged-in-as">
    <p class="logged-info">Username: <span id="username">{{username}}</span></p>
    <p class="logged-info">Id: <span id="username">{{userid}}</span></p>
    <p class="logged-info">Email: <span id="username">{{email}}</span></p>
  </div>
  {{/if}}
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <a href="/dashboard">My Activity</a>
    {{/if}}
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <aside>
      <h2>Enjoy posting!</h2>
      <br />
      {{#if loggedIn}}
      <div class="form-group">
        <a class="btn btn-primary" href="/new">Create a new Post</a>
      </div>
      {{/if}}
    </aside>
    <div class="article">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>